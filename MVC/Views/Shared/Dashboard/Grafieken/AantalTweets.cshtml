<label>Kies een soort item</label>

<select id="itemOpties">
    <option hidden selected>Selecteer een optie</option>
    <option>Persoon</option>
    <option>Organisatie</option>
    <option>Thema</option>
</select>

<div>
    <label id="lblKiezen"></label>
    <div id="itemAantalTweetsKeuzelijst"></div>
</div>

@*<div>
        <label>Selecteer een datumbereik</label>
        <div>
            Van:
            <input type="date" />
            Tot:
            <input type="date" />
        </div>
    </div>



    <div>
        <label>Hoe wilt u de evolutie weergeven?</label>
        <select id="selectEvolutieWeergave">
            <option hidden selected>Selecteer een optie</option>
            <option>Per dag</option>
            <option>Per week</option>
        </select>
    </div>*@

<div class="onzichtbaar" id="divToevoegenOpslaan">
    <button id="btnToevoegenOpslaan" class="btn btn-primary btn-algemene-layout">Opslaan</button>
</div>


<script>
    $("#itemOpties").change(function () {

        var keuze = $("#itemOpties").val();

        switch (keuze) {
            case "Persoon":

                $.get('@Url.Action("LaadPersonen1Item", "Dashboard")', {}, function (resultaat) {
                    $("#itemAantalTweetsKeuzelijst").html(resultaat);
                    $("#lblKiezen").html("Kies een persoon")
                });

            break;

            case "Organisatie":

                $.get('@Url.Action("LaadOrganisaties1Item", "Dashboard")', {}, function (resultaat) {
                    $("#itemAantalTweetsKeuzelijst").html(resultaat)
                    $("#lblKiezen").html("Kies een organisatie")

                 });

            break;

            case "Thema":

                $.get('@Url.Action("LaadThemas1Item", "Dashboard")', {}, function (resultaat) {
                    $("#itemAantalTweetsKeuzelijst").html(resultaat)
                    $("#lblKiezen").html("Kies een thema")

                 });

            break;
        }

        $("#divToevoegenOpslaan").show();

    });
</script>


<script>

    $("#btnToevoegenOpslaan").click(function () {

        var keuze = $("#itemOpties").val();
        var link;

        switch (keuze) {
            case "Persoon":
               
               @*link = '@Url.Action("LaadStaafdiagram", "Dashboard", new { grafiektitel = "replaceGrafiektitel", labels = "replaceLabels", data = "replaceData" })'
                        .replace("replaceGrafiektitel", $("#persoon1").val() + " - aantal tweets")
                        .replace("replaceLabels", ["seppe1", "seppe2", "seppe3", "seppe4", "seppe5"])
                    .replace("replaceData", [8, 4, 7, 15, 11]);*@

                link = '@Url.Action("LaadStaafdiagram", "Dashboard", new { grafiektitel = "replaceGrafiektitel" })'
                    .replace("replaceGrafiektitel", $("#persoon1").val() + " - aantal tweets");
                        
                    

            break;

            case "Organisatie":
                
                @*link = '@Url.Action("LaadStaafdiagram", "Dashboard", new { grafiektitel = "replaceGrafiektitel", labels = "replaceLabels", data = "replaceData" })'
                    .replace("replaceGrafiektitel", $("#organisatie1").val() + " - aantal tweets")
                        .replace("replaceLabels", ["seppe1", "seppe2", "seppe3", "seppe4", "seppe5"])
                    .replace("replaceData", [8, 4, 7, 15, 11]);*@

                link = '@Url.Action("LaadStaafdiagram", "Dashboard", new { grafiektitel = "replaceGrafiektitel", labels = "replaceLabels", data = "replaceData" })'
                    .replace("replaceGrafiektitel", $("#organisatie1").val() + " - aantal tweets");
                        
            break;

            case "Thema":

                @*link = '@Url.Action("LaadStaafdiagram", "Dashboard", new { grafiektitel = "replaceGrafiektitel", labels = "replaceLabels", data = "replaceData" })'
                    .replace("replaceGrafiektitel", $("#thema1").val())
                    .replace("replaceLabels", ["seppe1", "seppe2", "seppe3", "seppe4", "seppe5"])
                    .replace("replaceData", [8, 4, 7, 15, 11]);*@

                link = '@Url.Action("LaadStaafdiagram", "Dashboard", new { grafiektitel = "replaceGrafiektitel", labels = "replaceLabels", data = "replaceData" })'
                    .replace("replaceGrafiektitel", $("#thema1").val() + " - aantal tweets");

            break;
        }

        $.get(link, {}, function (resultaat) {
            $("#elementen").append("<div class=col-md-6>" + resultaat + "</div>");

        });

        $("#modalGrafiekToevoegen").modal('hide');

    });

</script>



@*<script>
    $("#btnToevoegenOpslaan").click(function () {

        var keuze = $("#itemOpties").val();
        var link;

        switch (keuze) {
            case "Persoon":

                link = '@Url.Action("LaadGetal", "Dashboard", new { item = "replace" })'.replace("replace", $("#persoon1").val());

            break;

            case "Organisatie":

                link = '@Url.Action("LaadGetal", "Dashboard", new { item = "replace" })'.replace("replace", $("#organisatie1").val());

            break;

            case "Thema":
                link = '@Url.Action("LaadGetal", "Dashboard", new { item = "replace" })'.replace("replace", $("#thema1").val());

            break;
        }

        $.get(link , {}, function (resultaat) {
            $("#elementen").append("<div class=col-md-6>" + resultaat + "</div>");

        });



        $("#modalGrafiekToevoegen").modal('hide');

            //var keuze = $("#persoon1").val();
            ////var link;

            //$("#elementen").append("<div class=col-md-6>" + "seppe" + "</div>");


            switch (keuze) {
                case "Alexander De Croo":

                    link = '@Url.Action("LaadStaafdiagram", "Dashboard", new { grafiektitel = "replaceGrafiektitel", labels = "replaceLabels", data = "replaceData" })'
                        .replace("replaceGrafiektitel", $("#persoon1").val())
                        .replace("replaceLabels", new string["seppe1", "seppe2", "seppe3", "seppe4", "seppe5"])
                        .replace("replaceData", new double[8, 4, 7, 15, 11]);
                break;

            }

            $.get(link, {}, function (resultaat) {
                $("#elementen").append("<div class=col-md-6>" + resultaat + "</div>");

            });

    </script>*@

