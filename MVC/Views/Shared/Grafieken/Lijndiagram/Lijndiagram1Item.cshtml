<div>
    <canvas id="@ViewBag.GrafiekId" height="300"></canvas>

    <button id="bewerken-@ViewBag.GrafiekId" class="btn btn-primary" onclick="grafiekBewerken(this.id)">
        <span class="glyphicon glyphicon-pencil"></span>
    </button>

    <button id="verwijderen-@ViewBag.GrafiekId" class="btn btn-primary" onclick="grafiekVerwijderen(this.id)">
        <span class="glyphicon glyphicon-remove"></span>
    </button>
</div>


<script>


    var grafiekId = @Html.Raw(Json.Encode(ViewBag.GrafiekId));

    var dagen = @Html.Raw(Json.Encode(ViewBag.XLabels));
    var grafiekTitel = @Html.Raw(Json.Encode(ViewBag.Grafiektitel));
    var data1 = @Html.Raw(Json.Encode(ViewBag.Data));
   
   
    GrafiekOpbouwen(grafiekId, grafiekTitel, "line", false, true, true, true, true, false, true, 90, 90, "Dag", "Aantal tweets", dagen,
        null, null, null, null, null, data1[0], null, null, null, null, ["#3e95cd"], null, null, null, null, ["#3e95cd"], null, null, null, null)



</script>

@*<script>
    function grafiekBewerken(id) {

        var lengteId = id.substring(id.indexOf("-") + 1, id.length - 1).length;
        var subid;

        if (lengteId == 0) {
            subid = id.substring(id.length - 1);
        } else if (lengteId > 0) {
            subid = id.substring(id.indexOf("-") + 1, id.length - 1);
        }


        var link = '@Html.Raw(Url.Action("LaadGrafiekAanpassen", "Dashboard",
        new { id = "replaceId" }
                     ))'.replace("replaceId", subid);


        $.get(link, {}, function (resultaat) {
            $("#divGrafiekAanpassen").html(resultaat);
        });


        $("#modalGrafiekAanpassen").modal();

    } 
</script>*@
